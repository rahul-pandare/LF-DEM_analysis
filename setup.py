from setuptools import setup, find_packages

setup(
    name='bidisperse_suspensions',       # replace with your project name
    version='0.1',
    packages=find_packages(),  # automatically finds `src/` as a package
)